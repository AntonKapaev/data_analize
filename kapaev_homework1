# Задание 1: пытаемся найти 2 максимальных значения в массиве
massa = [1, 9, 7, 2, 8, 1, 7, 2, 13, 1]
max1 =0
max2 =0

for i in range (0, len(massa)):
    if massa[i]>max1:
        max1=massa[i]

for j in range (0, len(massa)):
    if massa[j] > max2:
        if massa[j] < max1:
            max2=massa[j]
print("Самое максимальное значение", max1)
print("Второй максимум",max2)

#Задание 2: ищем ямы, их место и размер
y1=0 #номер ямы
yr1=0 #размер ямы

for h in range (1, len(massa)-1):
    if massa[h]<massa[h-1] and massa[h]<massa[h+1]:
        y1=h
        yr1=massa[h-1]+massa[h+1]-massa[h]
        print("номер ямы: ",y1," размер ямы:",yr1)


# Задание 3: ищем место с максимальным произведением 2-х значений из массива
max=0 # это переменная для максимального значения
maxpr1=0 #тут хранится номер первого места входящего в максимальное произведение
maxpr2=0 #тут хранится номер второго места входящего в максимальное произведение
for k in range(0,len(massa)):
    for l in range (1, len(massa)):
        if k != l:
            a=massa[k] * massa[l]
            if a > max:
                max=a
                maxpr1=k
                maxpr2=l

print ("максимальное произведение между элементами №", maxpr1, " и №", maxpr2, "и составляет:", max)
